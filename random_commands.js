/*
    let a = new Matrix(2, 2);
    a.randomize();
    a.print();

    function doubleIt(x){
        return x * 2;
    }

    a.map(doubleIt);
    a.print();

    let a = new Matrix(2, 3);
    let b = new Matrix(3, 2);
    a.randomize();
    b.randomize();
    console.table(a.matrix);
    console.table(b.matrix);
    let c = a.multiply(b.matrix);
    console.table(c.matrix);

    let d = a.transpose();
    console.table(a.matrix);
    console.table(d.matrix);

    function draw(){
    }

    brain = NeuralNetwork(3, 3, 1);
*/